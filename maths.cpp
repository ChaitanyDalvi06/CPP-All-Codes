#include <iostream>
using namespace std;
int main()
{
    int a, b, c, d, e, x, y, z, result;
    cout << "a+b+c+d/e * 1/x * 1/y * 1/z ";
    cin >> a >> b >> c >> d >> e >> x >> y >> z;
    cout << "Result = " << a + b + c + d / e * 1 / x * 1 / y * 1 / z << endl;
    return 0;
}